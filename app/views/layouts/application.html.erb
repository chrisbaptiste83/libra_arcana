<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <title><%= content_for(:title) || "Libra Arcana" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=IM+Fell+English:ital,wght@0,400;1,400&family=UnifrakturCook:wght@700&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="min-h-full flex flex-col bg-dark-bg text-gray-200">
    <%= render "shared/navbar" %>

    <% if notice.present? || alert.present? %>
      <div data-controller="flash" class="fixed top-20 right-4 z-50 max-w-sm">
        <% if notice.present? %>
          <div data-flash-target="message" class="flex items-center gap-3 px-4 py-3 rounded-lg bg-emerald-900/80 border border-emerald-700/50 text-emerald-200 shadow-lg backdrop-blur-sm mb-2">
            <svg class="w-5 h-5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/></svg>
            <span class="text-sm"><%= notice %></span>
          </div>
        <% end %>
        <% if alert.present? %>
          <div data-flash-target="message" class="flex items-center gap-3 px-4 py-3 rounded-lg bg-red-900/80 border border-red-700/50 text-red-200 shadow-lg backdrop-blur-sm">
            <svg class="w-5 h-5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.34 16.5c-.77.833.192 2.5 1.732 2.5z"/></svg>
            <span class="text-sm"><%= alert %></span>
          </div>
        <% end %>
      </div>
    <% end %>

    <main class="flex-1">
      <%= yield %>
    </main>

    <%= render "shared/footer" %>
  </body>
</html>
